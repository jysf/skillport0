//! Binary entry point.
//!
//! The CLI (arg parsing, `lint`, emitters, exit codes) is out of scope for
//! SPEC-001 and arrives in a later stage. This stub exists only so the crate
//! also builds as a binary; the substrate lives in the library (`lib.rs`).

fn main() {
    eprintln!("skillport: CLI not implemented yet (SPEC-001 ships the parser substrate only).");
}
