<!-- SPEC-012 coverage fixture: no YAML frontmatter block at all. -->
<!-- Fires: frontmatter.missing (error). -->

# No frontmatter here

This file has no opening `---` fence, so the whole file is treated as body.
