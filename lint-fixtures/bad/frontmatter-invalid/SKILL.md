---
- this
- is
- a YAML sequence, not a mapping
---

<!-- SPEC-012 coverage fixture: frontmatter parses as valid YAML but its root
     is a sequence, not a mapping. Fires: frontmatter.invalid (error). -->
